
#BioModule biolockj.module.seq.RarefySeqs

# This used to cause the pipeline to fail to form; but now this gives an appropriate error message.
#pipeline.defaultProps=FAAAAIL

# TODO: create a test for the bash system for how to handle a pipeline that never forms.
# And if you find something that does that... fix it; so it doesn't do that anymore.
