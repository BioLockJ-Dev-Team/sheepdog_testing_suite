
BioLockJ <VERSION> java help menu:
The biolockj.BioLockJ entry point is not intended to be called directly,
it should be called through the biolockj shell command, or the biolockj.launch.Launcher entry point.

Developers:
Usage: java -cp BioLockJ.jar biolockj.BioLockJ <args>
To run the program, args MUST include one of:
-config, -restartDir, -direct
Arguments
Named arguments for directories:
-projectDir, -restartDir
Other named arguments:
-config, -direct, -password
Flags:
-aws, -systemOut, -precheck, -unusedProps
Info-only arguments:
-help, -version

Users:
please use the biolockj command.
See: "biolockj --help"

BioLockJ <VERSION> java help menu:
The biolockj.BioLockJ entry point is not intended to be called directly,
it should be called through the biolockj shell command, or the biolockj.launch.Launcher entry point.

Developers:
Usage: java -cp BioLockJ.jar biolockj.BioLockJ <args>
To run the program, args MUST include one of:
-config, -restartDir, -direct
Arguments
Named arguments for directories:
-projectDir, -restartDir
Other named arguments:
-config, -direct, -password
Flags:
-aws, -systemOut, -precheck, -unusedProps
Info-only arguments:
-help, -version

Users:
please use the biolockj command.
See: "biolockj --help"
